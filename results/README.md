# results

This folder is a placeholder for optional tracked summary outputs. The main pipeline writes generated outputs under `target_transferability_lighter_outputs/`, which is ignored by Git by default.
